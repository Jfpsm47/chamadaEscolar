package com.example.chamada.repository;

import com.example.chamada.model.AlunoChamada;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AlunoChamadaRepository extends JpaRepository<AlunoChamada,Long> {
}
